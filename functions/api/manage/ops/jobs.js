import { runtime } from '../../../core/runtime.js';
export async function onRequestGet({ request, env }) { const app = runtime(env); const url = new URL(request.url); const items = await app.repository.listJobs({ limit: url.searchParams.get('limit'), cursor: url.searchParams.get('cursor'), status: url.searchParams.get('status'), channelId: url.searchParams.get('channelId'), operation: url.searchParams.get('operation') }); return Response.json({ items, nextCursor: items.length ? items.at(-1).cursor || null : null }); }
export async function onRequestPost({ request, env }) { const { jobId, action } = await request.json(); const app = runtime(env); if (action === 'retry') { const job = await app.jobs.retry(jobId); if (!job) return Response.json({ error: 'Job not found' }, { status: 404 }); await app.repository.audit({ id: `audit_${crypto.randomUUID()}`, action: 'job.retried', targetType: 'job', targetId: jobId }); return Response.json(job); } if (action === 'cancel') { const job = await app.jobs.cancel(jobId); if (!job) return Response.json({ error: 'Job not found' }, { status: 404 }); await app.repository.audit({ id: `audit_${crypto.randomUUID()}`, action: 'job.cancelled', targetType: 'job', targetId: jobId }); return Response.json(job); } return Response.json({ error: 'Unsupported action' }, { status: 400 }); }
