{
  "name": "cloudflare-imgbed",
  "version": "2.7.5",
  "type": "module",
  "scripts": {
    "ci-test": "concurrently --kill-others \"npm start\" \"wait-on http://localhost:8080 && mocha\"",
    "ci-test:docker": "concurrently --kill-others \"npm run start:docker\" \"wait-on http://localhost:8080 && mocha\"",
    "test": "mocha \"test/**/*.test.js\"",
    "test:unit": "npm run test",
    "test:integration": "npm run test",
    "lint": "node scripts/zero-cost-check.mjs",
    "build": "node deploy/worker/generate-routes.js",
    "check:zero-cost": "node scripts/zero-cost-check.mjs",
    "check:migrations": "node scripts/check-migrations.mjs",
    "check:secrets": "node scripts/secret-scan.mjs",
    "start": "npx wrangler pages dev ./frontend-dist --kv \"img_url\" --ip 0.0.0.0 --port 8080 --persist-to ./data",
    "start:docker": "node --import ./deploy/server/register.mjs deploy/server/index.js",
    "deploy:worker": "node deploy/worker/generate-toml.js --require-bindings && node deploy/worker/generate-routes.js && node scripts/validate-worker-deployment.mjs && npx wrangler deploy --config deploy/worker/wrangler.toml"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "3.726.1",
    "@cloudflare/pages-plugin-sentry": "^1.1.3",
    "@sentry/tracing": "^7.114.0"
  },
  "optionalDependencies": {
    "@hono/node-server": "^1.13.8",
    "better-sqlite3": "^11.9.1",
    "hono": "^4.7.4"
  },
  "overrides": {
    "fast-xml-parser": "^5.3.6"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "miniflare": "^4.20260708.1",
    "mocha": "^8.1.3",
    "wait-on": "^7.2.0",
    "wrangler": "^4.24.0"
  }
}
